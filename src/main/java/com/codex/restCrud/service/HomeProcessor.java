package com.codex.restCrud.service;

import org.springframework.stereotype.Service;

@Service
public class HomeProcessor
{
  /*  @Autowired
    UserDao userDao;
    
    @Autowired
	private CommentDao commentDao;
          
    public String getCloudTags()
    {
		StringBuilder result = new StringBuilder();
		result.append("<tags>");
		Iterator<Comment> i = commentDao.getComments(60).iterator();
		while(i.hasNext())
		{
		    Comment current = i.next();
		    result.append("<a href='http://localhost:8080/restCrud/search/");
		    result.append(current.getName());
		    result.append("' style='font-size:12pt;'>");
		    result.append(current.getName());
		    result.append("</a>");
		}
		result.append("</tags>");
		return result.toString();
    }*/
}
